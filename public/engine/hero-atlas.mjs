// Five authored views plus mirrored east views. See docs/ART-HANDOFF.md for
// the known side-view gait defect. Sol Max #4 Part 1 remaps flipped walk
// columns in animation.mjs so SW/W/NW lead with opposite contact frames
// until authored alternating strips replace the atlas.
export const heroAtlas = Object.freeze({
  cell: 384, columns: 6, rows: 5,
  anchor: { x: 192, y: 350 },
  worldSize: 144,
});
