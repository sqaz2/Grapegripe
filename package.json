{
  "name": "grapegripe",
  "version": "1.0.0",
  "private": true,
  "description": "A graphics-first mobile action journey through the Gripevine.",
  "scripts": {
    "test": "node scripts/validate.mjs && node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "type": "module"
}
